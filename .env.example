# AllDownloads Production Configuration
# Copy this file to .env and customize the values

# Server Configuration
DOMAIN=localhost
API_PORT=9780
UI_PORT=9779
AUTH_TOKEN=change-this-to-a-secure-random-token
BASE_URL=http://localhost

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:9779,http://localhost:9780

# Worker Configuration
REFRESH_CRON=@every 6h
HTTP_TIMEOUT=15s
MAX_CONCURRENT_FETCHES=6

# Security
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Example for production deployment:
# DOMAIN=yourdomain.com
# BASE_URL=https://yourdomain.com
# CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com
# AUTH_TOKEN=your-very-secure-random-token-here